type: object
required:
  - user
  - file
  - destination
properties:
  file:
    type: string
    format: binary
    description: File to be uploaded.
    x-constraints: "@ValidFile"
  destination:
    $ref: '../schema/Destination.yaml'