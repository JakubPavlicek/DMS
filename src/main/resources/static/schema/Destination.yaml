type: object
title: Destination
description: "The destination to store/move the document to."
properties:
  path:
    description: "The path to store/move the document to."
    type: string
    pattern: /$|(/[\w\-]+)+
    minLength: 1
    maxLength: 255
    example: /home
